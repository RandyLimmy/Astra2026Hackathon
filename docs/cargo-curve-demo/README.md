# Cargo lost on a sharp bend

[Watch the animation](animation.mp4). The trolley follows a marked 90-degree bend; the released load physically leaves the deck and contacts the floor.

Run `./launch_cargo_demo.command` from the repository root, then press Space. The slower control is `warehouse_curve_control_demo`.

The comparison executes the same route controller in both worlds. This is a synthetic physics demonstration, with no GPT-6-authored repair claimed. See [the full guide](../../simulator/CARGO_DEMO.md) for rendering and model investigation commands.
