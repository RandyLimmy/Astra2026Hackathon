# Cargo demo implementation plan

1. Physics lane: own simulator/platforms/warehouse.py, simulator/assets/warehouse.xml, tests/test_warehouse_cargo.py. Add staged cargo probes and force-triggered breakaway fault without changing existing preset mechanics. Expose private diagnostics/presentation information while preserving neutral public observations. Calibrate visible deviation and consistent failure law. Report public config integration contract before independent work proceeds.
2. Model-repair lane: own investigation/warehouse.py (or warehouse-specific package), candidate/warehouse_model.json, contracts/WAREHOUSE_MODEL.md, tests/test_warehouse_investigation.py. Build bounded declarative physical edits and persistent force/time-triggered equality changes, tool broker, neutral evidence, frozen evaluation, and optional fresh GPT-6 API runner reusing existing api.py. No arbitrary code execution, no raw user XML loading, no existing brake protocol refactor.
3. Leader: own simulator/cargo_demo.py, tests/test_cargo_demo.py, docs, isolated launcher. Generate a synchronized replay from physical nominal/reference trajectories with cargo position and center-of-mass overlays, phases, path separation and final measured error. Provide native launch plus a shareable animation artifact. User-facing GPT-6 patch track requires real candidate provenance. Wire to existing simulator CLI only if needed; use separate python -m simulator.cargo_demo otherwise.
4. Verify: physics tests, model validity/leak/freeze tests, media telemetry consistency, existing related regression tests, all tests once integrated, ruff/mypy/compile as applicable. Visual verdict per rendered iteration against staged storyboard; no reference image supplied. Record visual evidence in runs/cargo-demo and .omx/state/cargo-demo/ralph-progress.json.
5. Independent architecture, security and code-quality review; repair material findings. Update docs with exact launch/investigate/evaluate commands and distinguish prepared failure scenarios from measured GPT-6 repair success.

Constraints: No new dependencies. Preserve initial dirty drone/quadruped and plan files. No source patches to upstream MuJoCo. No commits requested. Candidate edits never touch reference state; no diagnostics, filenames or operator event labels in model observations. All generated artifacts under ignored runs/. Existing views and lab remain compatible.
