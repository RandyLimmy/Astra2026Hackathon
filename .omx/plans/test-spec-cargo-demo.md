# Test specification

See acceptance and verification in autopilot-spec.md and autopilot-impl.md. Prove physical onset, replay determinism, preserved existing controls, measured path error, candidate/reference isolation, frozen predictions before held-out reference, and clear faithful animation. No paid API call is required; a mocked API loop verifies plumbing but must be labeled as such.
