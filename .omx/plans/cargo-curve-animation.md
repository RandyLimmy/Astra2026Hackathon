# Visible curve and physical cargo spill

User correction: show a trolley deliberately taking a sharp bend on an obvious track, with its cargo falling off; straight travel followed by unexplained deviation is insufficient.

1. Physics lane owns warehouse.py/XML and new curve tests. Add separate curve probes/presets preserving previous experiments. Declare a straight approach, 90-degree bend, and exit before simulation. Drive using an observable-state route controller. Cargo has a complete physical set of relative degrees of freedom and contacts; after the scalar lateral restraint releases, it can leave the deck and strike the floor. No pose writes or scripted cargo trajectory. A slower identical-curve control keeps the cargo aboard.
2. Parent owns cargo_demo.py, launcher, presentation settings/doc additions and focused media tests. Show the intended path visibly in the world and chart; keep curve, trolley, and spilled load in camera. Compare the original prediction and measured attempt with the same controller, acknowledging differing feedback commands. Export physically sampled MP4/GIF and matched source provenance. Set new launcher/movie defaults to the visible curve.
3. Repair lane owns investigation/warehouse.py, contract, new curve investigation tests. Add explicit curve task selection and observed cargo pose. Score cargo motion/drop outcome and vehicle path on unseen speed/load trials. Preserve original investigation suite, hidden-rule separation, declarative repair, and no false GPT-6 success claims.
4. Verify physics: approach follows marked route, vehicle actually steers about90deg, cargo releases during bend and contacts ground, slow control stays aboard, no solver warnings, no state teleport, deterministic replay and half timestep sensitivity. Verify curve candidate baseline fails and known developer fixture succeeds only as a test. Review/render representative approach, bend, falling, and final frames before final output. Focus regression on warehouse/repair/replay; avoid repeating unrelated lengthy whole-repo tests.

New paired tests distinguish matched feedback policy from identical open-loop commands. This is a wheeled trolley on a marked track, not a physically rail-constrained railway train.
